<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>فروشگاه VPN</title>

    <script src="https://telegram.org/js/telegram-web-app.js"></script>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            padding: 20px 15px;
            font-family: Tahoma, Arial, sans-serif;
            background: var(--tg-theme-bg-color);
            color: var(--tg-theme-text-color);
        }

        .container {
            max-width: 500px;
            margin: auto;
        }

        .header {
            text-align: center;
            padding: 20px 10px;
        }

        .logo {
            font-size: 55px;
        }

        h1 {
            margin: 10px 0 5px;
            font-size: 25px;
        }

        .welcome {
            opacity: 0.7;
            font-size: 14px;
        }

        .menu {
            display: flex;
            flex-direction: column;
            gap: 12px;
            margin-top: 15px;
        }

        .menu-button {
            width: 100%;
            padding: 18px;
            border: none;
            border-radius: 16px;
            background: var(--tg-theme-secondary-bg-color);
            color: var(--tg-theme-text-color);
            font-size: 17px;
            font-weight: bold;
            text-align: right;
            cursor: pointer;
            transition: transform 0.1s;
        }

        .menu-button:active {
            transform: scale(0.97);
        }

        .emoji {
            font-size: 24px;
            margin-left: 10px;
            vertical-align: middle;
        }

        .footer {
            text-align: center;
            margin-top: 25px;
            opacity: 0.5;
            font-size: 12px;
        }
    </style>
</head>

<body>

<div class="container">

    <div class="header">
        <div class="logo">🔐</div>

        <h1>فروشگاه VPN</h1>

        <div class="welcome" id="welcome">
            خوش آمدید 👋
        </div>
    </div>

    <div class="menu">

        <button class="menu-button" onclick="openSection('buy')">
            <span class="emoji">🛒</span>
            خرید اشتراک
        </button>

        <button class="menu-button" onclick="openSection('services')">
            <span class="emoji">📦</span>
            سرویس‌های من
        </button>

        <button class="menu-button" onclick="openSection('wallet')">
            <span class="emoji">💰</span>
            کیف پول
        </button>

        <button class="menu-button" onclick="openSection('test')">
            <span class="emoji">🔑</span>
            اکانت تست
        </button>

        <button class="menu-button" onclick="openSection('renew')">
            <span class="emoji">🔄</span>
            تمدید سرویس
        </button>

        <button class="menu-button" onclick="openSection('tutorial')">
            <span class="emoji">🎓</span>
            آموزش
        </button>

        <button class="menu-button" onclick="openSection('support')">
            <span class="emoji">💬</span>
            پشتیبانی
        </button>

    </div>

    <div class="footer">
        فروش امن و سریع سرویس VPN
    </div>

</div>

<script>

    const tg = window.Telegram.WebApp;

    tg.ready();
    tg.expand();

    // نمایش نام کاربر تلگرام
    const user = tg.initDataUnsafe?.user;

    if (user) {
        document.getElementById("welcome").innerText =
            "سلام " + (user.first_name || "دوست من") + " 👋";
    }

    function openSection(section) {

        tg.showAlert("این بخش به‌زودی فعال می‌شود 🚀");

    }

</script>

</body>
</html>
